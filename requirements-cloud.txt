# Облачный Jupyter (DataSphere / Colab-like): НЕ трогаем системный torch/jupyter.
# Предустановленный torch+cu118 обычно уже есть — им и пользуемся.
#
# Установка:
#   %pip install -r requirements-cloud.txt
# Затем Kernel → Restart.
#
# Конфликты pip с tensorflow/fastai/google-* можно игнорировать —
# они не нужны для этого проекта.

# --- ядро сегментации ---
segmentation-models-pytorch>=0.3.3,<0.6
timm>=0.9.12,<1.1
transformers>=4.36.0,<5.0
# transformers 4.x дружелюбнее к torch 2.0–2.6; 5.x тоже ок, но тянет больше зависимостей

albumentations>=1.3.1,<1.5
# 1.4.x не тянет opencv 5 / numpy 2 так агрессивно, как 2.0+

numpy>=1.22.0,<1.27
opencv-python-headless>=4.8.0,<4.11
Pillow>=9.4.0,<11
tqdm>=4.65.0
PyYAML>=6.0
scipy>=1.10.0,<1.14
matplotlib>=3.5.0,<3.9

# логи (лёгкий pin, чтобы не ломать tensorflow protobuf)
tensorboard>=2.12.0,<2.15
protobuf>=3.20.3,<5

# OCR номеров КП (torch уже в образе)
easyocr>=1.7.0,<2.0

# VLM-детекция КП (OpenAI / Yandex AI Studio); для Yandex см. YC_API_KEY + YC_FOLDER_ID
openai>=1.40.0
# yandex-ai-studio-sdk>=0.15.0  # опционально, надёжнее multimodal


# НЕ ставим из этого файла:
# torch, torchvision  — уже в образе
# jupyter, ipywidgets — уже в образе
